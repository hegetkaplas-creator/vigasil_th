<!-- Deprecated: prefer StickyCTA.astro (position: sticky, iframe-safe). Kept for compatibility if imported elsewhere. -->
<div class="sticky-bar-anchor">
  <div class="sticky-bar">
    <div class="sticky-copy">
      <div class="mini">เป็นส่วนตัวสูงสุด · เก็บเงินปลายทาง</div>
      <strong>CBD Vigasil · <span style="color: var(--brand);">990 ฿</span></strong>
    </div>
    <button type="button" class="sticky-btn" data-scroll-to-hero-order>จองโปรโมชั่นตอนนี้</button>
  </div>
</div>

<style>
  .sticky-bar-anchor {
    position: sticky;
    bottom: 0;
    z-index: 17;
    padding: 10px 14px 14px;
    pointer-events: none;
  }

  .sticky-bar {
    pointer-events: auto;
    border-radius: 24px;
    padding: 10px 12px;
    display: flex;
    align-items: center;
    gap: 12px;
    backdrop-filter: blur(32px) saturate(180%);
    -webkit-backdrop-filter: blur(32px) saturate(180%);
    background: linear-gradient(
      135deg,
      rgba(255, 255, 255, 0.9) 0%,
      rgba(240, 253, 250, 0.85) 50%,
      rgba(255, 255, 255, 0.88) 100%
    );
    border: 2px solid rgba(255, 255, 255, 0.85);
    box-shadow:
      0 18px 48px rgba(13, 148, 136, 0.18),
      0 8px 22px rgba(245, 158, 11, 0.1),
      inset 0 2px 4px rgba(255, 255, 255, 0.95),
      inset 0 -2px 4px rgba(52, 211, 153, 0.08);
    animation: stickyBarIn 0.45s ease-out;
  }

  @keyframes stickyBarIn {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .sticky-copy {
    min-width: 0;
    flex: 1;
  }
  .sticky-copy .mini {
    font-size: 11px;
    color: var(--text-dim);
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: 2px;
  }
  .sticky-copy strong {
    display: block;
    font-size: 15px;
    letter-spacing: -0.02em;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .sticky-btn {
    border: 0;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    min-height: 48px;
    padding: 0 18px;
    border-radius: 16px;
    color: white;
    font-weight: 800;
    background: linear-gradient(135deg, var(--brand) 0%, var(--tropical-green) 50%, var(--teal) 100%);
    box-shadow:
      0 16px 35px rgba(13, 148, 136, 0.24),
      0 6px 18px rgba(52, 211, 153, 0.18),
      inset 0 1px 2px rgba(255, 255, 255, 0.25);
    transition: transform 0.16s ease;
    white-space: nowrap;
    position: relative;
    overflow: hidden;
  }

  .sticky-btn::after {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.3);
    transform: translate(-50%, -50%);
    transition: width 0.6s ease, height 0.6s ease;
  }

  .sticky-btn:hover::after {
    width: 300px;
    height: 300px;
  }

  @media (max-width: 720px) {
    .sticky-bar-anchor {
      padding: 8px 8px 10px;
    }
    .sticky-bar {
      padding: 8px 10px;
    }
    .sticky-btn {
      min-height: 40px;
      padding: 0 16px;
      font-size: 13px;
      border-radius: 14px;
    }
    .sticky-copy strong {
      font-size: 13px;
    }
    .sticky-copy .mini {
      font-size: 10px;
    }
  }
</style>
