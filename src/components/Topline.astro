<div class="topline">
  <div class="container">
    <div class="topline-inner glass">
      <div class="brand">
        <img src="/assets/img/logo-5.png" alt="CBD Vigasil Logo" style="height: 42px; width: auto;">
        <div class="brand-copy">
          <div class="brand-title">CBD Vigasil</div>
          <div class="brand-sub">ข้อมูลผลิตภัณฑ์อย่างเป็นทางการ</div>
        </div>
      </div>
      <div class="top-meta">
        <strong>เลขที่จดแจ้ง อย.</strong>
        11-1-13958-5-0571 · 15 แคปซูล · GMP/GHPs
      </div>
    </div>
  </div>
</div>

<style>
  .topline { padding: 14px 0 10px; }

  .topline-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    border-radius: 999px;
    padding: 10px 14px;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 12px;
    min-width: 0;
  }

  .brand-title {
    font-size: 15px;
    font-weight: 800;
    letter-spacing: -.02em;
  }

  .brand-sub {
    font-size: 12px;
    color: var(--text-dim);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .top-meta {
    text-align: right;
    font-size: 12px;
    color: var(--text-dim);
  }

  .top-meta strong {
    display: block;
    color: var(--text);
    font-size: 13px;
    font-weight: 700;
  }

  @media (max-width: 720px) {
    .topline-inner { border-radius: 20px; }
    .top-meta { display: none; }
  }
</style>
