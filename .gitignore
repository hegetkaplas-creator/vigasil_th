# ── OS ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor / AI agent configs (local only, never deploy) ──────────────
.cursor/
.clinerules
.roo/
.cline/
.windsurf/

# ── Cloudflare / Wrangler ────────────────────────────────────────────
.wrangler/

# ── Node ──────────────────────────────────────────────────────────────
node_modules/

# ── Astro build output ───────────────────────────────────────────────
dist/
.astro/

# ── Legacy asset path (now in public/assets/) ────────────────────────
/assets/

# ── QA artifacts ──────────────────────────────────────────────────────
.playwright-mcp/
*-smoke-*.png

# ── Design source files (for dev context only, not deployed) ──────────
**/*.psd
**/*.ai
**/*.sketch
**/*.fig
**/*.xd
**/*.mov
**/*.MOV
**/*.avi
assets/Packaging/

# ── Product / internal docs (for dev context only, not deployed) ───────
docs/
_source/
design/
source/
reference/

# ── Draft / scratch pages ─────────────────────────────────────────────
*_raw.html
*_draft.html
*_old.html

.dev.vars*
!.dev.vars.example
.env*
!.env.example
